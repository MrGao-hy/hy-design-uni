import type IProps from './typing'

const defaultProps: IProps = {
  modelValue: false,
  content: '',
  placement: 'bottom',
  offset: 0,
  showClose: false,
  disabled: false,
  mode: 'normal',
}

export default defaultProps
