/**
 * 加载样式类型
 *
 * @default spinner
 *
 * @values
 * spinner-旋转加载样式
 * circle-圆环加载样式
 * semicircle-半圆加载样式
 * dots-跳动圆点加载样式
 * bars-音波柱加载样式
 */
export type LoadingMode = 'spinner' | 'circle' | 'semicircle' | 'dots' | 'bars'
