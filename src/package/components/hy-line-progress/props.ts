import type IProps from "./typing";

const defaultProps: IProps = {
  activeColor: "",
  inactiveColor: "",
  percentage: 0,
  showText: true,
  height: 8,
  fontSize: "",
};

export default defaultProps;
