import type IProps from "./typing";

const defaultProps: IProps = {
  labelPosition: "left",
  labelWidth: "auto",
  labelAlign: "left",
  symbol: false,
  borderBottom: false,
  right: false,
  itemHeight: 40,
  columns: [],
  formData: {},
};

export default defaultProps;
